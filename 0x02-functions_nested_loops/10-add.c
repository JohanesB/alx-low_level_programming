#include "main.h"
/**
 * add - check the code
 * Description: a function that adds two integers and returns the result.
 * Return: sum
 */
int add(int x, int y)
{
	int sum;

	sum = x + y;
	return (sum);
}
